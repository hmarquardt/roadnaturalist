# Offline preparation

`build-ecoregions.py` prepares the first pilot dataset from pinned EPA Oregon Level III/IV shapefile ZIPs. See `docs/ECOREGIONS.md` for exact commands, source digests, schema, and scope. Keep raw source caches out of Git; generated small GeoParquet files are committed so local analysis works without production data hosting.
